<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.project.mapper.ProductMapper">
    <select id="selectAllProducts" resultType="Product">
        SELECT * FROM product ORDER BY id DESC
    </select>

    <select id="selectProductById" resultType="Product">
        SELECT * FROM product WHERE id = #{id}
    </select>

    <insert id="insertProduct" parameterType="Product" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product(productName, quantity)
        VALUES (#{productName}, #{quantity})
    </insert>

    <update id="updateProduct" parameterType="Product">
        UPDATE product
        SET productName = #{productName},
            quantity = #{quantity}
        WHERE id = #{id}
    </update>

    <delete id="deleteProductById" parameterType="long">
        DELETE FROM product WHERE id = #{id}
    </delete>

    <select id="searchProducts" resultType="Product">
        SELECT * FROM product
        <where>
            <trim prefixOverrides="OR">
                <foreach item="word" collection="keyword.split(' ')" separator=" ">
                    OR productName LIKE CONCAT('%', #{word}, '%')
                </foreach>
            </trim>
        </where>
        ORDER BY id DESC
    </select>

</mapper>