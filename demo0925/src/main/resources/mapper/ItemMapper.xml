<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo0925.mapper.ItemMapper">

    <insert id="insertItem" parameterType="com.example.demo0925.domain.ItemDTO" useGeneratedKeys="true" keyProperty="itemId">
        INSERT INTO items (title, brand, description, price)
        VALUES (#{title}, #{brand}, #{description}, #{price})
    </insert>

    <select id="selectAllItems" resultType="com.example.demo0925.domain.ItemDTO">
        SELECT
        item_id,
        title,
        brand,
        description,
        price
        FROM items
    </select>

    <select id="selectItemById" parameterType="int" resultType="com.example.demo0925.domain.ItemDTO">
        SELECT
        item_id,
        title,
        brand,
        description,
        price
        FROM items
        WHERE item_id = #{itemId}
    </select>

    <update id="updateItem" parameterType="com.example.demo0925.domain.ItemDTO">
        UPDATE items
        SET
        title = #{title},
        brand = #{brand},
        description = #{description},
        price = #{price}
        WHERE item_id = #{itemId}
    </update>

    <delete id="deleteItem" parameterType="int">
        DELETE FROM items
        WHERE item_id = #{itemId}
    </delete>

</mapper>